html

  head
    script(src="/io/example-socket/socket.io.js")
    script(type='text/javascript').

      var socket = io({path: '/io/example-socket'});

      socket.emit('message', 'first message');

      setInterval(function(){
        socket.emit('message', 'hello');
      }, 2000);

      socket.on('message', function(msg){
        msg = new Date() + '<br />' + 'message from server: ' + msg;
        var el = document.getElementById('message');
        el.innerHTML += msg + '<br />';
      });

      socket.on('connect', function() {
        console.log('client connected');
      });

  body
    h1 MicroMono Socket.IO Example

    h3 Message from server (emits every 2 seconds):
    div#message
